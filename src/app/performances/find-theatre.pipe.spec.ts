import { FindTheatrePipe } from './find-theatre.pipe';

describe('FindTheatrePipe', () => {
  it('create an instance', () => {
    const pipe = new FindTheatrePipe();
    expect(pipe).toBeTruthy();
  });
});
